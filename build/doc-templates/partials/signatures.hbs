{{~new-line~}}
{{#each signatures~}}
	```typescript {{new-line~}}
	{{../access}}{{../name}}{{#if generics}}<{{#each generics}}{{this}}{{^if @last}}, {{/if}}{{/each}}>{{/if}}({{#each arguments}} {{name}}{{#if optional}}?{{/if}}:{{{type}}}{{^if @last}},{{/if}} {{/each}}){{#with returns}}:{{{type}}}{{/with}} {{~new-line~}}
	```

	{{#with description~}}
		{{{trim this}}}
	{{/with}}

	{{#if arguments~}}
		*Parameters*

		{{#each arguments~}}
			- {{name}}{{#with description}}: {{{this}}}{{/with}}
		{{/each}}

	{{/if}}
{{/each}}