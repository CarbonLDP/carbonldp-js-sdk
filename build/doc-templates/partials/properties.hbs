{{~new-line~}}
{{#each properties~}}
	```typescript {{new-line~}}
	{{../access}}{{name}}{{#if optional}}?{{/if}}:{{{type}}} {{new-line~}}
	```

	{{#with description~}}
		{{{trim this}}}
	{{/with}}

{{^if @last}}--{{/if}}

{{/each}}