<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8">
	<title>Carbon LDP JavaScript SDK - Docs</title>
	<link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.2.4/semantic.min.css">
	<link rel="stylesheet" type="text/css" href="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/9.7.0/styles/darcula.min.css">
	<style>
		.main.container {
			margin-top: 5em;
		}

		.symbol {
			border: 0 !important;
			padding-right: 0 !important;
			padding-left: 0 !important;
			margin-left: -1em !important;
			left: calc(-1rem - 0.75em) !important;
		}

		.symbol::after {
			border: 0 !important;
		}

		.symbol__anchor {
			text-decoration: none;
			opacity: 0.20;
		}

		.symbol__anchor .icon {
			margin-right: 0 !important;
		}

		.symbol__anchor:hover {
			opacity: 1;
		}

		.doc-object__header {
		}

		.doc-object__header:not(.doc-object__header-section).doc-header .doc-header__label {
			margin: 0;
			padding-left: 0;
		}

		.doc-object__header-section .symbol {
			margin-left: 0 !important;
		}

		.doc-header {

		}

		.doc-header__label {
		}

		.doc-label {
			display: inline-block;
		}

		code, .code-label {
			background-color: rgba(0, 0, 0, 0.08);
			border-radius: 3px;
			vertical-align: baseline;
			padding: .25em .50em;
		}

		.doc-label > .doc-label__main {
			padding: .5833em .833em;
		}

		.doc-label > .doc-label__secondary {
			opacity: 0.60;
		}

		.doc-content {
		}

		.doc-content__description, .doc-description > p {
			font-size: 15px;
		}

		.elements-list {
		}

		.elements-list .item:before {
			margin-top: 0.5em !important;
		}

		.elements-list__item-type {
			width: 28px;
			text-align: center;
			display: table-cell !important;
			vertical-align: middle !important;
		}

		.elements-list_single .item:before {
			margin-top: 0.2em !important;
		}

		.elements-list_single .content {
			vertical-align: middle !important;
			font-size: 1.13em !important;
		}

		.elements-list code {
			padding: .07em .25em;
		}

		.doc-module__default-exports strong {
			padding-left: 1.68571429em;
		}

		.justified > p {
			text-align: justify;
		}

	</style>
</head>
<body>

	<div class="ui main container">
		<h1 class="ui header">Carbon LDP JavaScript SDK API Reference</h1>
		<div class="ui divider"></div>

		{{>header level=2 path="Classes and Interfaces" name="Classes and Interfaces"}}
		<div class="ui bulleted list elements-list elements-list_single">
			{{#toc-tree modules}}
				<div class="item">
					<span class="ui {{#is type "I"}}orange{{else}}blue{{/is}} label icon elements-list__item-type">{{type}}</span>
					<a class="content" href="#{{sanitize path}}">{{path}}</a>
				</div>
			{{/toc-tree}}
		</div>
		<br>


		{{#each modules}}
			<div class="ui basic grey segment doc-module">
				{{>header path=this.path name=this.path isModule=true}}

				{{#with description}}
					<div class="doc-content doc-module__content">
						<div class="doc-content__description doc-description">
							{{>text-markdown element=this}}
						</div>
					</div>
				{{/with}}

				{{#with default-export}}
					<div class="doc-module__default-exports">
						<strong>Default Export</strong>:
						<a href="#{{sanitize name}}">{{name}}</a>
					</div>
				{{/with}}

				{{#or this.interfaces this.classes}}
					{{>header level=3 path=(concat path "--Classes and Interfaces") name="Classes and Interfaces of the Module" }}
					<div class="ui bulleted list elements-list">
						{{#combineElements this}}
							<div class="item">
								<span class="ui {{#if isInterface}}orange{{else}}blue{{/if}} label icon elements-list__item-type">{{#if isClass}}C{{else}}I{{/if}}</span>
								<div class="content">
									<a class="header" href="#{{sanitize element.path}}">{{element.path}}</a>
									{{#with element.description}}
										<div class="description">{{>text-markdown element=this noParagraph=true}}</div>{{/with}}
								</div>
							</div>
						{{/combineElements}}
					</div>
				{{/or}}

				{{#if reexports~}}
					{{> reexports}}
				{{/if}}

				{{#if enums~}}
					{{#each enums}}
						{{>enum this}}
					{{/each}}
				{{/if}}

				{{#if properties}}
					{{>header level=3 path=(concat path "--Properties") name="Properties" }}

					{{>properties properties=properties.static access="#"}}

					{{>properties properties=properties.instance access="."}}
				{{/if}}

				{{#if methods}}
					{{>header level=3 path=(concat path "--Methods") name="Methods" }}

					{{>methods methods=methods.static access="#" }}

					{{>methods methods=methods.instance access="." }}
				{{/if}}

			</div>

			{{#combineElements this}}
				<div class="ui basic {{#if isClass}}blue{{else}}orange{{/if}} segment doc-object">

					{{>header path=element.path name=element.path}}

					{{#with description}}
						<div class="doc-content doc-object__content">
							<div class="doc-content__description doc-description">
								{{>text-markdown element=this}}
							</div>
						</div>
					{{/with}}

					{{#with element}}
						{{#if interfces}}
							<div class="ui list">
								<div class="item">
									<i class="icon"></i>
									<div class="content">
										<span class="header">Implements</span>
										{{#each interfces}}
											<div class="list">
												<div class="item">
													<i class="icon"></i>
													<div class="content">
														<span class="description">
															{{>text-markdown element=this noParagraph=true}}
														</span>
													</div>
												</div>
											</div>
										{{/each}}
									</div>
								</div>
							</div>
						{{/if}}

						{{#if super-classes}}
							<div class="ui list">
								<div class="item">
									<i class="icon"></i>
									<div class="content">
										<span class="header">Extends</span>
										{{#each super-classes}}
											<div class="list">
												<div class="item">
													<i class="icon"></i>
													<div class="content">
														<span class="description">
															{{>text-markdown element=name noParagraph=true}}
														</span>
													</div>
												</div>
											</div>
										{{/each}}
									</div>
								</div>
							</div>
						{{/if}}

						{{#if reexports~}}
							{{> reexports}}
						{{/if}}

						{{#with constructors~}}
							{{>header level=3 path=(concat ../path "--Constructor") name="Constructor" }}

							{{>signatures name=../name }}
						{{/with}}

						{{#if properties}}
							{{>header level=3 path=(concat path "--Properties") name="Properties" }}

							{{#if ../isClass}}
								{{>properties properties=properties.static access="#"}}

								{{>properties properties=properties.instance access="."}}
							{{else}}
								{{>properties properties=properties access="."}}
							{{/if}}
						{{/if}}

						{{#if methods}}
							{{>header level=3 path=(concat path "--Methods") name="Methods" }}
							{{#if ../isClass}}
								{{>methods methods=methods.static access="#"}}

								{{>methods methods=methods.instance access="."}}
							{{else}}
								{{>methods methods=methods access="." }}
							{{/if}}
						{{/if}}

					{{/with}}

				</div>
			{{/combineElements}}

		{{!--#each classes}}

			{{#with decorated-object~}}
				#### <a name="{{sanitize ../path}}-Decorated-Object"/>Decorated Object
				{{#if type~}}
					**Interfaces:** {{#each type}}[{{this}}](#{{sanitize this}}){{^if @last}}, {{/if}}{{/each}}
				{{/if}}

				{{#with description~}}
					> {{{trim this}}}
				{{/with}}

				{{#if properties ~}}
					##### <a name="{{sanitize ../path}}-Decorated-Object-Properties"/>Properties

					{{~>properties properties=properties.instance access=""}}
				{{/if}}

				{{#if methods~}}
					##### <a name="{{sanitize ../path}}-Decorated-Object-Methods"/>Methods

					{{~>methods methods=methods.instance access=""}}
				{{/if}}
			{{/with}}
		{{/each--}}

		{{/each}}
		<br><br>

	</div>

	<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.1.1/jquery.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/semantic-ui/2.2.4/semantic.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/9.7.0/highlight.min.js"></script>
	<script src="https://cdnjs.cloudflare.com/ajax/libs/highlight.js/9.7.0/languages/typescript.min.js"></script>
	<script>hljs.initHighlightingOnLoad();</script>
</body>
</html>