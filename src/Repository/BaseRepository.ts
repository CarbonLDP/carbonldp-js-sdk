import { Pointer } from "../Pointer/Pointer";

/**
 * Base properties to create a {@link Repository}.
 */
export interface BaseRepository {
}

/**
 * Base properties to create a {@link $Repository}.
 */
export interface $BaseRepository extends Pointer {
}
