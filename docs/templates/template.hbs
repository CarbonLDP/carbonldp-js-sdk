# CarbonLDP SDK API description

{{#each modules}}

	{{#with path~}}
		## <a name="{{sanitize this}}" />{{this}}
	{{/with}}

	{{#with description~}}
		> {{{this}}}
	{{/with}}

	{{#if reexports~}}
		### Reexports

		{{new-line~}}
		| Export name | Original Location | {{new-line~}}
		| --- | --- |
		{{#each reexports~}}
			| {{name}} | [{{originalLocation}}](#{{sanitize originalLocation}}) |
		{{/each}}
	{{/if}}

	{{#each classes}}

		{{#with path~}}
			### Class {{this}}
		{{/with}}

		{{#with description~}}
			> {{{this}}}
		{{/with}}

		{{#if reexports~}}
			#### Reexports

			{{new-line~}}
			| Export name | Original Location | {{new-line~}}
			| --- | --- |
			{{#each reexports ~}}
				| {{name}} | [{{originalLocation}}](#{{sanitize originalLocation}}) |
			{{/each}}
		{{/if}}

		{{#with constructors~}}
			#### Constructor

			{{#each signatures~}}
				```typescript {{new-line~}}
				{{../../name}}({{#each arguments}} {{name}}{{#if optional}}?{{/if}}:{{{type}}}{{^if @last}},{{/if}} {{/each}}) {{new-line~}}
				```
				{{#with description~}}
					{{{this}}}
				{{/with}}

				{{#if arguments~}}
					**Parameters**

					{{#each arguments~}}
						- {{name}} {{#with description}}: {{this}}{{/with}}
					{{/each}}
				{{/if~}}
			{{/each~}}
		{{/with}}

		{{#if properties~}}
			#### Properties

			{{#each properties.static~}}
				```typescript {{new-line~}}
				static {{name}}:{{{type}}} {{new-line~}}
				```

				{{#with description~}}
					{{{this}}}
				{{/with}}
			{{/each}}

			{{#each properties.static~}}
				```typescript {{new-line~}}
				{{name}}:{{{type}}} {{new-line~}}
				```

				{{#with description~}}
					{{{this}}}
				{{/with}}
			{{/each}}
		{{/if}}

		{{#if methods~}}
			#### Methods

			{{#each methods.static~}}
				##### {{name}}

				{{#each signatures~}}
					```typescript {{new-line~}}
					static {{../name}}({{#each arguments}} {{name}}{{#if optional}}?{{/if}}:{{{type}}}{{^if @last}},{{/if}} {{/each}}):{{{returns.type}}} {{new-line~}}
					```

					{{#with description~}}
						{{{this}}}
					{{/with}}

					{{#if arguments~}}
						**Parameters**

						{{#each arguments~}}
							- {{name}} {{#with description}}: {{this}}{{/with}}
						{{/each}}

					{{/if}}
				{{/each}}
			{{/each}}

			{{#each methods.instance~}}
				##### {{name}}

				{{#each signatures~}}
					```typescript {{new-line~}}
					{{../name}}({{#each arguments}} {{name}}{{#if optional}}?{{/if}}:{{{type}}}{{^if @last}},{{/if}} {{/each}}):{{{returns.type}}} {{new-line~}}
					```

					{{#with description~}}
						{{{this}}}
					{{/with}}

					{{#if arguments~}}
						**Parameters**

						{{#each arguments~}}
							- {{name}} {{#with description}}: {{this}}{{/with}}
						{{/each}}

					{{/if}}
				{{/each}}
			{{/each}}
		{{/if}}
	{{/each}}
{{/each}}