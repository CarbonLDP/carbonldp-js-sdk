# CarbonLDP SDK API Description

{{#each modules}}

	{{#with path~}}
		## <a name="{{sanitize this}}" />{{this}}
	{{/with}}

	{{#with description~}}
		> {{{trim this}}}
	{{/with}}

	{{#with default-export~}}
		**Default export:** [{{name}}](#{{sanitize name}})
		{{#with description~}}
			> {{{trim this}}}
		{{/with}}
	{{/with}}

	{{~> reexports}}

	{{#if enums~}}
		### Enums
		{{#each enums}}
			{{~>enum this}}
		{{/each}}
	{{/if}}

	{{#if properties~}}
		### Module Properties

		{{~> properties properties=properties.static access="static " }}

		{{~> properties properties=properties.instance access="" }}
	{{/if}}

	{{#if methods~}}
		### Module Methods

		{{~>methods methods=methods.static access="static " }}

		{{~>methods methods=methods.instance access="" }}
	{{/if}}

	{{#each classes}}

		{{#with path~}}
			### <a name="{{sanitize this}}" />Class {{this}}
		{{/with}}

		{{#if super-classes~}}
			**Extends:** {{#each super-classes}}[{{name}}](#{{sanitize name}}){{^if @last}}, {{/if}}{{/each}}
		{{/if}}

		{{#with description~}}
			> {{{trim this}}}
		{{/with}}

		{{~> reexports}}

		{{#with constructors~}}
			#### Constructor

			{{~>signatures name=../name }}
		{{/with}}

		{{#if properties~}}
			#### Properties

			{{~>properties properties=properties.static access="static " }}

			{{~>properties properties=properties.instance access="" }}
		{{/if}}

		{{#if methods~}}
			#### Methods

			{{~>methods methods=methods.static access="static " }}

			{{~>methods methods=methods.instance access="" }}
		{{/if}}

		{{#with decorated-object~}}
			#### Decorated Object
			{{#if type~}}
				**Interfaces:** {{#each type}}[{{this}}](#{{sanitize this}}){{^if @last}}, {{/if}}{{/each}}
			{{/if}}

			{{#with description~}}
				> {{{trim this}}}
			{{/with}}

			{{#if properties ~}}
				##### Properties

				{{~>properties properties=properties.instance access=""}}
			{{/if}}

			{{#if methods~}}
				##### Methods

				{{~>methods methods=methods.instance access=""}}
			{{/if}}
		{{/with}}
	{{/each}}
{{/each}}